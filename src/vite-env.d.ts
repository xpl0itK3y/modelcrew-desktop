/// <reference types="vite/client" />

// Injected by the Vite/Vitest `define` option; absent under other tools, so
// consumers must guard with `typeof __APP_VERSION__`.
declare const __APP_VERSION__: string | undefined;
